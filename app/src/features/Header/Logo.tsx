import { Typography } from "@mui/material";

const Logo = () => (
  <Typography
    component="h1"
    id="logo"
    sx={{
      fontSize: 32,
      color: "rgba(241, 241, 241, 0.73)",
      fontFamily: "Rubik",
    }}>
    syncoli
  </Typography>
);

export default Logo;
